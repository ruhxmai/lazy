---
id: sql-acid
category: Backend
daily: true
---

# Что такое ACID в базах данных?

## Ответ

**ACID** — четыре свойства, которые гарантируют надёжность транзакций.

- **Atomicity (атомарность)** — транзакция выполняется полностью или не выполняется вообще
- **Consistency (согласованность)** — БД переходит из одного валидного состояния в другое, не нарушая ограничения (constraints, foreign keys)
- **Isolation (изоляция)** — параллельные транзакции не видят промежуточные (незакоммиченные) изменения друг друга
- **Durability (долговечность)** — после `COMMIT` данные сохраняются даже при сбое сервера

```sql
BEGIN;
UPDATE accounts SET balance = balance - 100 WHERE id = 1;
UPDATE accounts SET balance = balance + 100 WHERE id = 2;
COMMIT; -- если что-то упадёт раньше — ROLLBACK вернёт всё назад
```

Если сбой произойдёт между двумя `UPDATE`, благодаря **Atomicity** ни один из них не применится — счета останутся в исходном состоянии.

> Без ACID перевод денег мог бы списаться с одного счёта и не дойти до другого — деньги "исчезнут".
